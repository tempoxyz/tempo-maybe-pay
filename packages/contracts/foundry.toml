[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["lib"]
solc_version = "0.8.26"
optimizer = true
optimizer_runs = 200
via_ir = true
evm_version = "cancun"
tempo = true

[rpc_endpoints]
moderato = "${TEMPO_TESTNET_RPC_URL}"
tempo = "${TEMPO_MAINNET_RPC_URL}"
